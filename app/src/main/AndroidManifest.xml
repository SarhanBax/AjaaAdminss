<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ajaaadmin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Todayspayment"></activity>
        <activity android:name=".RefundActivity" />
        <activity android:name=".Recyclerhistoryofdriver" />
        <activity android:name=".Notapproved" />
        <activity android:name=".DriverfilterActivity" />
        <activity android:name=".SpecificPassengerActivity" />
        <activity android:name=".Specificdriverdetail" />
        <activity android:name=".DriversDetails" />
        <activity android:name=".PassengerDetails" />
        <activity android:name=".nav" />
        <activity android:name=".Login" />
        <activity android:name=".Registration" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>